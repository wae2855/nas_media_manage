## Config Cleanup Archive

- Date: 2026-06-06
- Reason: move historical runtime config backup out of active `config/` directory so AI and developers only see the live config facts by default.

Archived files in this folder are retained for traceability only and must not be treated as current runtime configuration.
